<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ระบบครูเวรประจำวัน</title>
<link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&family=Kanit:wght@400;500;600;700&display=swap" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
<style>
:root {
  --teal-dark:#00695C;--teal:#26A69A;--teal-light:#4DB6AC;--teal-pale:#E0F2F1;
  --lime:#AED581;--amber:#FFCA28;--orange:#FF8A65;--red:#EF5350;
  --purple:#4A2C6D;--blue:#0288D1;--yellow-bg:#FFFDE7;
  --gray-50:#f8f9fa;--gray-100:#f1f5f4;--gray-200:#e8efee;
  --gray-400:#9eb3b0;--gray-600:#546E7A;--gray-800:#263238;
  --shadow-sm:0 2px 8px rgba(38,166,154,.08);
  --shadow-md:0 4px 20px rgba(38,166,154,.12);
  --shadow-lg:0 8px 40px rgba(38,166,154,.16);
  --radius:16px;--radius-sm:10px;--sidebar-width:220px;
}
*{margin:0;padding:0;box-sizing:border-box}
html{scroll-behavior:smooth}
body{font-family:'Sarabun',sans-serif;background:var(--yellow-bg);color:var(--gray-800);
  height:100vh;display:flex;overflow:hidden;font-size:17px}

/* SIDEBAR */
.hamburger{display:none;position:fixed;top:12px;left:12px;z-index:200;
  width:40px;height:40px;background:var(--teal);border-radius:10px;border:none;
  cursor:pointer;align-items:center;justify-content:center;color:#fff;font-size:18px;
  box-shadow:0 2px 10px rgba(0,0,0,.2)}
.sidebar-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:99}
.sidebar-overlay.active{display:block}
.sidebar{width:var(--sidebar-width);background:linear-gradient(170deg,#00695C 0%,#26A69A 50%,#4A2C6D 100%);
  display:flex;flex-direction:column;flex-shrink:0;overflow-y:auto;z-index:100;
  transition:transform .3s ease}
.sidebar-header{padding:20px 14px 14px;border-bottom:1px solid rgba(255,255,255,.15);
  background:rgba(255,255,255,.07);display:flex;flex-direction:column;
  align-items:center;text-align:center;gap:8px}
.logo-wrap{width:82px;height:82px;border-radius:50%;overflow:hidden;
  border:3px solid rgba(255,255,255,.3);display:flex;align-items:center;justify-content:center}
.logo-wrap img{width:100%;height:100%;object-fit:cover}
.logo-wrap .logo-fallback{font-size:30px;color:#fff}
.logo-text h1{font-family:'Kanit',sans-serif;font-size:14px;font-weight:700;color:#fff;line-height:1.3}
.logo-text p{font-size:12px;color:rgba(255,255,255,.6);margin-top:2px}
.sidebar-school{margin-top:2px;padding:6px 10px;background:rgba(255,255,255,.1);border-radius:8px;
  font-size:12px;color:rgba(255,255,255,.8);display:flex;align-items:center;gap:5px;width:100%;justify-content:center}
nav{flex:1;padding:10px 8px}
.nav-group{margin-bottom:10px}
.nav-group-label{font-size:10px;font-weight:700;color:rgba(255,255,255,.4);
  letter-spacing:1.5px;text-transform:uppercase;padding:0 10px;margin-bottom:4px}
.nav-item{display:flex;align-items:center;gap:9px;padding:8px 10px;border-radius:8px;
  color:rgba(255,255,255,.78);cursor:pointer;transition:all .2s;font-size:14px;
  font-weight:500;margin-bottom:1px;border:1px solid transparent}
.nav-item:hover{background:rgba(255,255,255,.12);color:#fff}
.nav-item.active{background:rgba(255,255,255,.2);color:#fff;
  border-color:rgba(255,255,255,.25);box-shadow:0 2px 8px rgba(0,0,0,.15)}
.nav-icon{width:28px;height:28px;border-radius:7px;display:flex;align-items:center;
  justify-content:center;font-size:14px;background:rgba(255,255,255,.1);flex-shrink:0}
.nav-item.active .nav-icon{background:rgba(255,255,255,.25)}
.sidebar-footer{padding:12px 8px;border-top:1px solid rgba(255,255,255,.12)}
.status-pill{display:flex;align-items:center;gap:8px;padding:8px 12px;
  background:rgba(255,255,255,.08);border-radius:10px;font-size:13px;color:rgba(255,255,255,.7)}
.status-dot{width:8px;height:8px;border-radius:50%;background:#69F0AE;animation:pulse 2s infinite}
@keyframes pulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.6;transform:scale(1.3)}}

/* MAIN */
main{flex:1;overflow-y:auto;padding:28px;background:var(--yellow-bg)}
.page{display:none}.page.active{display:block}
.page-header{display:flex;justify-content:space-between;align-items:flex-start;
  margin-bottom:28px;gap:16px;flex-wrap:wrap}
.page-title{display:flex;align-items:center;gap:14px}
.page-title-icon{width:52px;height:52px;border-radius:14px;display:flex;
  align-items:center;justify-content:center;font-size:23px;color:#fff;flex-shrink:0}
.page-title h2{font-family:'Kanit',sans-serif;font-size:22px;font-weight:600;color:var(--gray-800)}
.page-title p{font-size:13px;color:var(--gray-600);margin-top:2px}

/* CARDS */
.cards-row{display:grid;gap:16px;margin-bottom:24px}
.cards-4{grid-template-columns:repeat(4,1fr)}
.cards-3{grid-template-columns:repeat(3,1fr)}
.cards-2{grid-template-columns:repeat(2,1fr)}
.stat-card{border-radius:var(--radius);padding:20px;color:#fff;position:relative;
  overflow:hidden;cursor:default;transition:transform .25s,box-shadow .25s}
.stat-card:hover{transform:translateY(-4px);box-shadow:var(--shadow-lg)}
.stat-card::before{content:'';position:absolute;top:-30px;right:-30px;
  width:120px;height:120px;border-radius:50%;background:rgba(255,255,255,.12)}
.stat-card-label{font-size:12px;font-weight:500;opacity:.85;display:flex;
  align-items:center;gap:6px;margin-bottom:10px}
.stat-card-value{font-family:'Kanit',sans-serif;font-size:36px;font-weight:700;line-height:1}
.stat-card-sub{font-size:12px;opacity:.75;margin-top:8px}
.stat-card-icon{position:absolute;right:20px;top:20px;font-size:28px;opacity:.35}
.g1{background:linear-gradient(135deg,#26A69A,#00897B)}
.g2{background:linear-gradient(135deg,#7CB342,#558B2F)}
.g3{background:linear-gradient(135deg,#F57C00,#E65100)}
.g4{background:linear-gradient(135deg,#E53935,#B71C1C)}
.g5{background:linear-gradient(135deg,#7B1FA2,#4A148C)}
.g6{background:linear-gradient(135deg,#0277BD,#01579B)}

/* GLASS CARD */
.glass{background:rgba(255,255,255,.92);border-radius:var(--radius);
  border:1px solid rgba(255,255,255,.6);box-shadow:var(--shadow-sm);
  padding:24px;margin-bottom:20px}
.glass-header{display:flex;justify-content:space-between;align-items:center;margin-bottom:20px}
.glass-title{display:flex;align-items:center;gap:10px;font-family:'Kanit',sans-serif;
  font-size:17px;font-weight:600;color:var(--gray-800)}
.icon-box{width:34px;height:34px;border-radius:9px;display:flex;
  align-items:center;justify-content:center;font-size:16px;color:#fff}

/* BUTTONS */
.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;
  padding:10px 20px;border-radius:var(--radius-sm);font-family:'Sarabun',sans-serif;
  font-size:15px;font-weight:600;cursor:pointer;border:none;transition:all .2s;text-decoration:none}
.btn-primary{background:linear-gradient(135deg,var(--teal),var(--teal-dark));color:#fff;
  box-shadow:0 2px 8px rgba(38,166,154,.35)}
.btn-primary:hover{transform:translateY(-1px);box-shadow:0 4px 16px rgba(38,166,154,.4)}
.btn-success{background:linear-gradient(135deg,#43A047,#2E7D32);color:#fff}
.btn-danger{background:linear-gradient(135deg,#E53935,#C62828);color:#fff}
.btn-warning{background:linear-gradient(135deg,#F9A825,#F57F17);color:#fff}
.btn-outline{background:transparent;color:var(--teal);border:2px solid var(--teal)}
.btn-outline:hover{background:var(--teal);color:#fff}
.btn-sm{padding:7px 14px;font-size:12px;border-radius:8px}
.btn-lg{padding:14px 28px;font-size:16px;border-radius:14px}

/* FORM */
.form-grid{display:grid;gap:16px}
.form-grid-2{grid-template-columns:1fr 1fr}
.form-grid-3{grid-template-columns:1fr 1fr 1fr}
.form-group{display:flex;flex-direction:column;gap:6px}
.form-label{font-size:14px;font-weight:600;color:var(--gray-600);display:flex;align-items:center;gap:5px}
.form-control{padding:11px 14px;border-radius:var(--radius-sm);border:1.5px solid var(--gray-200);
  font-family:'Sarabun',sans-serif;font-size:15px;color:var(--gray-800);background:#fff;
  outline:none;transition:all .2s}
.form-control:focus{border-color:var(--teal);box-shadow:0 0 0 3px rgba(38,166,154,.12)}

/* TABLE */
.data-table{width:100%;border-collapse:collapse;font-size:14px}
.data-table thead th{background:linear-gradient(90deg,var(--teal-pale),#f0faf9);
  color:var(--teal-dark);font-weight:700;padding:12px 16px;text-align:left;
  border-bottom:2px solid var(--teal-light);white-space:nowrap}
.data-table tbody tr{transition:background .15s}
.data-table tbody tr:hover{background:rgba(38,166,154,.04)}
.data-table tbody td{padding:12px 16px;border-bottom:1px solid var(--gray-100);vertical-align:middle}
.data-table tbody tr:last-child td{border-bottom:none}

/* BADGE */
.badge{display:inline-flex;align-items:center;gap:5px;padding:4px 12px;
  border-radius:20px;font-size:12px;font-weight:700;white-space:nowrap}
.badge-success{background:#E8F5E9;color:#2E7D32}
.badge-warning{background:#FFF8E1;color:#F57F17}
.badge-danger{background:#FFEBEE;color:#C62828}
.badge-info{background:#E3F2FD;color:#1565C0}
.badge-gray{background:#ECEFF1;color:#546E7A}

/* CAMERA */
.camera-modal{display:none;position:fixed;inset:0;background:rgba(0,0,0,.92);
  z-index:5000;flex-direction:column;align-items:center;justify-content:center;gap:16px}
.camera-modal.open{display:flex}
.camera-modal video{width:100%;max-width:480px;max-height:60vh;
  border-radius:16px;object-fit:cover;border:3px solid var(--teal)}
.camera-modal canvas{display:none}
.camera-toolbar{display:flex;gap:12px;flex-wrap:wrap;justify-content:center}
.shutter-btn{width:72px;height:72px;border-radius:50%;border:4px solid #fff;
  background:rgba(255,255,255,.2);cursor:pointer;display:flex;align-items:center;
  justify-content:center;font-size:28px;color:#fff;transition:all .15s;flex-shrink:0}
.shutter-btn:hover{background:rgba(255,255,255,.35);transform:scale(1.05)}
.camera-switch-btn{width:48px;height:48px;border-radius:50%;border:2px solid rgba(255,255,255,.5);
  background:rgba(255,255,255,.1);cursor:pointer;display:flex;align-items:center;
  justify-content:center;font-size:18px;color:#fff;transition:all .15s}
.camera-switch-btn:hover{background:rgba(255,255,255,.25)}
.photo-preview-box{border:2px dashed var(--gray-200);border-radius:var(--radius);
  padding:24px;text-align:center;cursor:pointer;transition:all .2s;background:var(--gray-50);
  min-height:140px;display:flex;align-items:center;justify-content:center;flex-direction:column;gap:8px}
.photo-preview-box:hover{border-color:var(--teal);background:var(--teal-pale)}
.photo-preview-box img{width:100%;max-height:180px;object-fit:cover;border-radius:10px;display:none}
.photo-preview-box.has-photo img{display:block}
.photo-preview-box.has-photo .photo-placeholder{display:none}
.photo-placeholder{display:flex;flex-direction:column;align-items:center;gap:6px}

/* GPS CARD */
.gps-card{background:linear-gradient(135deg,#E0F7FA,#B2EBF2);border:1.5px solid #80DEEA;
  border-radius:var(--radius-sm);padding:14px 18px;display:flex;
  align-items:center;gap:12px;margin-bottom:14px;flex-wrap:wrap}

/* CALENDAR */
.calendar-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:3px}
.cal-header{text-align:center;font-size:12px;font-weight:700;color:var(--teal-dark);padding:8px 0}
.cal-day{min-height:70px;border-radius:8px;border:1px solid var(--gray-100);
  padding:6px;font-size:13px;background:#fff;transition:all .15s;cursor:pointer}
.cal-day:hover{border-color:var(--teal);box-shadow:0 2px 8px rgba(38,166,154,.15)}
.cal-day.other-month{background:var(--gray-50);opacity:.5}
.cal-day.today{border-color:var(--teal);background:var(--teal-pale)}
.cal-day .day-num{font-weight:700;font-size:14px;color:var(--gray-800)}
.duty-dot{width:6px;height:6px;border-radius:50%;display:inline-block;margin:1px}

/* QUICK ACTIONS */
.quick-actions{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-bottom:24px}
.quick-card{background:#fff;border-radius:var(--radius);padding:18px;border-left:4px solid;
  cursor:pointer;transition:all .2s;box-shadow:var(--shadow-sm);display:flex;align-items:center;gap:14px}
.quick-card:hover{transform:translateY(-3px);box-shadow:var(--shadow-md)}
.quick-icon{width:42px;height:42px;border-radius:10px;display:flex;align-items:center;
  justify-content:center;font-size:18px;color:#fff;flex-shrink:0}
.quick-text p{font-size:14px;font-weight:600;color:var(--gray-800)}
.quick-text span{font-size:11px;color:var(--gray-600)}

/* LOADING */
.loading-overlay{position:fixed;inset:0;background:rgba(255,255,255,.82);
  backdrop-filter:blur(4px);z-index:9999;display:none;align-items:center;
  justify-content:center;flex-direction:column;gap:14px}
.spinner{width:50px;height:50px;border:4px solid var(--teal-pale);border-top-color:var(--teal);
  border-radius:50%;animation:spin .8s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
.loading-text{font-size:15px;font-weight:600;color:var(--teal)}

/* TOAST */
.toast-container{position:fixed;top:20px;right:20px;z-index:10000;
  display:flex;flex-direction:column;gap:10px}
.toast{background:#fff;border-radius:12px;padding:14px 18px;
  box-shadow:0 4px 20px rgba(0,0,0,.12);display:flex;align-items:center;gap:12px;
  min-width:260px;max-width:90vw;border-left:4px solid;animation:slideIn .3s ease}
.toast.success{border-color:#43A047}.toast.error{border-color:#E53935}.toast.info{border-color:var(--teal)}
@keyframes slideIn{from{transform:translateX(120%);opacity:0}to{transform:translateX(0);opacity:1}}

/* PROGRESS */
.progress{background:var(--gray-100);border-radius:20px;height:8px;overflow:hidden}
.progress-bar{height:100%;border-radius:20px;transition:width .5s}

/* BOTTOM NAV */
.bottom-nav{display:none;position:fixed;bottom:0;left:0;right:0;
  background:linear-gradient(135deg,#00695C,#26A69A);z-index:150;
  padding:6px 0 env(safe-area-inset-bottom,6px);box-shadow:0 -4px 20px rgba(0,0,0,.15)}
.bottom-nav-inner{display:flex;justify-content:space-around;align-items:center}
.bottom-nav-item{display:flex;flex-direction:column;align-items:center;gap:2px;
  padding:6px 8px;border-radius:10px;color:rgba(255,255,255,.65);cursor:pointer;
  font-size:10px;font-weight:600;min-width:50px;transition:all .2s}
.bottom-nav-item i{font-size:19px}
.bottom-nav-item.active{color:#fff;background:rgba(255,255,255,.18)}

/* RESPONSIVE */
@media(max-width:1100px){
  :root{--sidebar-width:190px}
  .cards-4{grid-template-columns:repeat(2,1fr)}
  .quick-actions{grid-template-columns:repeat(2,1fr)}
  .form-grid-3{grid-template-columns:1fr 1fr}
}
@media(max-width:768px){
  body{overflow:hidden}
  .hamburger{display:flex}
  .sidebar{position:fixed;top:0;left:0;bottom:0;width:240px;transform:translateX(-100%);
    z-index:100;box-shadow:4px 0 24px rgba(0,0,0,.25)}
  .sidebar.open{transform:translateX(0)}
  main{padding:16px 14px 80px;width:100%;overflow-y:auto;-webkit-overflow-scrolling:touch}
  .bottom-nav{display:block}
  .page-header{flex-direction:column;margin-bottom:16px;gap:10px}
  .cards-4,.cards-3{grid-template-columns:repeat(2,1fr)}
  .cards-2{grid-template-columns:1fr}
  .stat-card-value{font-size:29px}
  .quick-actions{grid-template-columns:repeat(2,1fr);gap:10px}
  .form-grid-2,.form-grid-3{grid-template-columns:1fr}
  .glass{padding:16px}
  .toast-container{top:12px;right:12px;left:12px}
  .toast{min-width:unset;width:100%}
  .cal-day{min-height:48px;padding:4px}
  .data-table thead th,.data-table tbody td{padding:8px 10px;font-size:12px}
}
@media(max-width:380px){
  .stat-card-value{font-size:25px}.stat-card{padding:14px}
  main{padding:12px 10px 80px}
}

.checkin-grid{display:grid;grid-template-columns:1fr 1fr;gap:24px}
.checkin-right{display:flex;flex-direction:column;gap:20px}
@media(max-width:768px){.checkin-grid{grid-template-columns:1fr}}
</style>
</head>
<body>

<!-- LOADING -->
<div class="loading-overlay" id="loadingOverlay">
  <div class="spinner"></div>
  <div class="loading-text">กำลังโหลด...</div>
</div>
<div class="toast-container" id="toastContainer"></div>

<!-- CAMERA MODAL -->
<div class="camera-modal" id="cameraModal">
  <div style="color:#fff;font-family:'Kanit',sans-serif;font-size:16px;display:flex;align-items:center;gap:8px">
    <i class="fas fa-camera"></i> ถ่ายรูปหลักฐาน
    <span id="camFacingLabel" style="font-size:12px;opacity:.7">(กล้องหลัง)</span>
  </div>
  <video id="camVideo" autoplay muted playsinline></video>
  <canvas id="camCanvas"></canvas>
  <div class="camera-toolbar">
    <button class="camera-switch-btn" onclick="switchCamera()" title="สลับกล้อง"><i class="fas fa-sync-alt"></i></button>
    <button class="shutter-btn" onclick="captureFromCamera()" title="ถ่ายรูป"><i class="fas fa-circle"></i></button>
    <button class="camera-switch-btn" onclick="closeCameraModal()" title="ยกเลิก"><i class="fas fa-times"></i></button>
  </div>
  <div style="font-size:12px;color:rgba(255,255,255,.5)">กดวงกลมเพื่อถ่ายรูป · กด ✕ เพื่อยกเลิก</div>
</div>

<!-- ADMIN MODAL -->
<div id="adminModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.6);
  z-index:6000;align-items:center;justify-content:center">
  <div style="background:#fff;border-radius:20px;padding:32px;width:90%;max-width:380px;
    box-shadow:0 20px 60px rgba(0,0,0,.3);text-align:center">
    <div style="width:64px;height:64px;border-radius:50%;background:linear-gradient(135deg,var(--teal),var(--teal-dark));
      display:flex;align-items:center;justify-content:center;margin:0 auto 16px;font-size:26px;color:#fff">
      <i class="fas fa-lock"></i>
    </div>
    <h3 style="font-family:'Kanit',sans-serif;font-size:18px;margin-bottom:6px">ยืนยันตัวตน Admin</h3>
    <p style="font-size:13px;color:var(--gray-600);margin-bottom:20px">กรุณาใส่รหัสผ่านเพื่อเข้าถึงการตั้งค่า</p>
    <input type="password" id="adminPwdInput" class="form-control"
      placeholder="รหัสผ่าน Admin"
      style="text-align:center;font-size:18px;letter-spacing:4px;margin-bottom:16px"
      onkeydown="if(event.key==='Enter')verifyAdminPwd()">
    <div id="adminPwdError" style="color:#E53935;font-size:13px;margin-bottom:12px;display:none">
      <i class="fas fa-times-circle"></i> รหัสผ่านไม่ถูกต้อง
    </div>
    <div style="display:flex;gap:10px">
      <button class="btn btn-outline" style="flex:1" onclick="closeAdminModal()">ยกเลิก</button>
      <button class="btn btn-primary" style="flex:1" id="adminSubmitBtn" onclick="verifyAdminPwd()">
        <i class="fas fa-unlock"></i> เข้าสู่ระบบ
      </button>
    </div>
  </div>
</div>

<!-- HAMBURGER + OVERLAY -->
<button class="hamburger" onclick="toggleSidebar()"><i class="fas fa-bars"></i></button>
<div class="sidebar-overlay" id="sidebarOverlay" onclick="closeSidebar()"></div>

<!-- SIDEBAR -->
<aside class="sidebar" id="sidebar">
  <div class="sidebar-header">
    <div class="logo-wrap">
      <img id="logoImg" src="" alt="โลโก้" style="display:none"
           onerror="this.style.display='none';document.getElementById('logoFallback').style.display='flex'">
      <div class="logo-fallback" id="logoFallback" style="display:flex"><i class="fas fa-chalkboard-teacher"></i></div>
    </div>
    <div class="logo-text">
      <h1>ระบบครูเวรประจำวัน</h1>
      <p>พัฒนาโดย : พ่อมดตะนอย</p>
    </div>
    <div class="sidebar-school"><i class="fas fa-school"></i> <span id="schoolNameDisplay">โรงเรียนพ่อมดไอที</span></div>
  </div>
  <nav>
    <div class="nav-group">
      <div class="nav-group-label">หลัก</div>
      <div class="nav-item active" onclick="switchPage('dashboard')"><div class="nav-icon"><i class="fas fa-chart-pie"></i></div><span>แดชบอร์ด</span></div>
      <div class="nav-item" onclick="switchPage('checkin')"><div class="nav-icon"><i class="fas fa-user-clock"></i></div><span>บันทึกลงชื่อเข้าเวร</span></div>
      <div class="nav-item" onclick="switchPage('calendar')"><div class="nav-icon"><i class="fas fa-calendar-alt"></i></div><span>ปฏิทินเวร</span></div>
      <div class="nav-item" onclick="switchPage('schedule')"><div class="nav-icon"><i class="fas fa-table"></i></div><span>ตารางเวรทั้งปี</span></div>
    </div>
    <div class="nav-group">
      <div class="nav-group-label">รายงาน</div>
      <div class="nav-item" onclick="switchPage('logs')"><div class="nav-icon"><i class="fas fa-history"></i></div><span>บันทึกการเข้าเวร</span></div>
      <div class="nav-item" onclick="switchPage('stats')"><div class="nav-icon"><i class="fas fa-bar-chart"></i></div><span>สถิติครู</span></div>
      <div class="nav-item" onclick="switchPage('report')"><div class="nav-icon"><i class="fas fa-file-pdf"></i></div><span>รายงาน PDF</span></div>
    </div>
    <div class="nav-group">
      <div class="nav-group-label">จัดการ</div>
      <div class="nav-item" onclick="switchPage('teachers')"><div class="nav-icon"><i class="fas fa-users"></i></div><span>ข้อมูลครู</span></div>
      <div class="nav-item" onclick="switchPage('addschedule')"><div class="nav-icon"><i class="fas fa-calendar-plus"></i></div><span>เพิ่มตารางเวร</span></div>
      <div class="nav-item" onclick="switchPage('settings')"><div class="nav-icon"><i class="fas fa-cog"></i></div><span>ตั้งค่าระบบ</span></div>
    </div>
  </nav>
  <div class="sidebar-footer">
    <div class="status-pill">
      <div class="status-dot"></div>
      <span id="sysStatus">กำลังเชื่อมต่อ...</span>
    </div>
  </div>
</aside>

<!-- BOTTOM NAV -->
<nav class="bottom-nav">
  <div class="bottom-nav-inner">
    <div class="bottom-nav-item active" onclick="switchPage('dashboard');setBottomActive(this)"><i class="fas fa-chart-pie"></i><span>หน้าหลัก</span></div>
    <div class="bottom-nav-item" onclick="switchPage('checkin');setBottomActive(this)"><i class="fas fa-user-clock"></i><span>เข้าเวร</span></div>
    <div class="bottom-nav-item" onclick="switchPage('calendar');setBottomActive(this)"><i class="fas fa-calendar-alt"></i><span>ปฏิทิน</span></div>
    <div class="bottom-nav-item" onclick="switchPage('logs');setBottomActive(this)"><i class="fas fa-history"></i><span>บันทึก</span></div>
    <div class="bottom-nav-item" onclick="switchPage('settings');setBottomActive(this)"><i class="fas fa-cog"></i><span>ตั้งค่า</span></div>
  </div>
</nav>

<main>

<!-- DASHBOARD -->
<div class="page active" id="page-dashboard">
  <div class="page-header">
    <div class="page-title">
      <div class="page-title-icon g1"><i class="fas fa-chart-pie"></i></div>
      <div><h2>แดชบอร์ดภาพรวม</h2><p id="dashDateDisplay">กำลังโหลด...</p></div>
    </div>
    <div style="display:flex;gap:10px;flex-wrap:wrap">
      <button class="btn btn-outline" onclick="loadDashboard()"><i class="fas fa-sync-alt"></i> รีเฟรช</button>
      <button class="btn btn-primary" onclick="switchPage('checkin')"><i class="fas fa-user-check"></i> บันทึกลงชื่อเวร</button>
    </div>
  </div>
  <div class="cards-row cards-4">
    <div class="stat-card g1"><i class="fas fa-calendar-day stat-card-icon"></i><div class="stat-card-label"><i class="fas fa-users"></i> เวรวันนี้</div><div class="stat-card-value" id="ds-total">-</div><div class="stat-card-sub">รายการในตาราง</div></div>
    <div class="stat-card g2"><i class="fas fa-check-circle stat-card-icon"></i><div class="stat-card-label"><i class="fas fa-user-check"></i> เข้าเวรแล้ว</div><div class="stat-card-value" id="ds-checkedin">-</div><div class="stat-card-sub">คน</div></div>
    <div class="stat-card g3"><i class="fas fa-times-circle stat-card-icon"></i><div class="stat-card-label"><i class="fas fa-user-times"></i> รอ/ขาดเวร</div><div class="stat-card-value" id="ds-absent">-</div><div class="stat-card-sub">คน</div></div>
    <div class="stat-card g5"><i class="fas fa-chart-line stat-card-icon"></i><div class="stat-card-label"><i class="fas fa-calendar-alt"></i> เวรเดือนนี้</div><div class="stat-card-value" id="ds-month">-</div><div class="stat-card-sub">รายการ</div></div>
  </div>
  <div class="quick-actions">
    <div class="quick-card" style="border-color:var(--teal)" onclick="switchPage('checkin')"><div class="quick-icon" style="background:var(--teal)"><i class="fas fa-user-clock"></i></div><div class="quick-text"><p>ลงชื่อเข้าเวร</p><span>บันทึกพร้อมรูป+GPS</span></div></div>
    <div class="quick-card" style="border-color:var(--lime)" onclick="switchPage('calendar')"><div class="quick-icon" style="background:#7CB342"><i class="fas fa-calendar-alt"></i></div><div class="quick-text"><p>ปฏิทินเวร</p><span>ดูตารางรายเดือน</span></div></div>
    <div class="quick-card" style="border-color:var(--amber)" onclick="exportExcel()"><div class="quick-icon" style="background:#F9A825"><i class="fas fa-file-excel"></i></div><div class="quick-text"><p>Export Excel</p><span>ดาวน์โหลดรายงาน</span></div></div>
    <div class="quick-card" style="border-color:var(--red)" onclick="switchPage('stats')"><div class="quick-icon" style="background:#E53935"><i class="fas fa-bar-chart"></i></div><div class="quick-text"><p>สถิติครู</p><span>ดูข้อมูลรายบุคคล</span></div></div>
  </div>
  <div style="display:grid;grid-template-columns:2fr 1fr;gap:20px">
    <div class="glass">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g1"><i class="fas fa-chart-bar"></i></div> สถิติ 7 วันล่าสุด</div></div>
      <canvas id="chartWeekly" height="90"></canvas>
    </div>
    <div class="glass">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g5"><i class="fas fa-chart-pie"></i></div> สัดส่วนวันนี้</div></div>
      <canvas id="chartToday" height="150"></canvas>
    </div>
  </div>
  <div class="glass">
    <div class="glass-header">
      <div class="glass-title"><div class="icon-box g6"><i class="fas fa-history"></i></div> กิจกรรมล่าสุด</div>
      <button class="btn btn-sm btn-outline" onclick="switchPage('logs')">ดูทั้งหมด <i class="fas fa-arrow-right"></i></button>
    </div>
    <div style="overflow-x:auto">
      <table class="data-table">
        <thead><tr><th>วันที่</th><th>ชื่อครู</th><th>ประเภทเวร</th><th>เวลาเข้า</th><th>เวลาออก</th><th>GPS</th><th>สถานะ</th></tr></thead>
        <tbody id="recentLogsBody"></tbody>
      </table>
    </div>
  </div>
</div>

<!-- CHECK-IN -->
<div class="page" id="page-checkin">
  <div class="page-header">
    <div class="page-title">
      <div class="page-title-icon g2"><i class="fas fa-user-clock"></i></div>
      <div><h2>ลงชื่อเข้า-ออกเวร</h2><p>บันทึกพร้อมรูปภาพและตรวจสอบตำแหน่ง GPS</p></div>
    </div>
  </div>
  <div class="checkin-grid">
    <div class="glass" style="border-left:4px solid var(--teal)">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g1"><i class="fas fa-sign-in-alt"></i></div> บันทึกเข้าเวร</div></div>
      <div class="gps-card" id="gpsCard">
        <i class="fas fa-map-marker-alt" style="font-size:22px;color:var(--teal)"></i>
        <div style="flex:1;min-width:0">
          <div style="font-size:13px;font-weight:600" id="gpsStatusText">กดปุ่มเพื่อตรวจสอบ GPS</div>
          <div style="font-size:11px;color:var(--gray-600)" id="gpsCoordText">ยังไม่ได้รับตำแหน่ง</div>
        </div>
        <button class="btn btn-sm btn-primary" onclick="getGPS()"><i class="fas fa-crosshairs"></i> ตรวจสอบ</button>
      </div>
      <form onsubmit="handleCheckIn(event)" class="form-grid" style="gap:14px">
        <input type="hidden" id="ciLat"><input type="hidden" id="ciLng">
        <input type="hidden" id="ciPhotoBase64">
        <div class="form-grid form-grid-2">
          <div class="form-group"><label class="form-label"><i class="fas fa-id-card"></i> รหัสครู</label><input class="form-control" id="ciTeacherId" placeholder="เช่น T001" required></div>
          <div class="form-group"><label class="form-label"><i class="fas fa-user"></i> ชื่อ-นามสกุล</label><input class="form-control" id="ciTeacherName" placeholder="ชื่อเต็ม" required></div>
        </div>
        <div class="form-grid form-grid-2">
          <div class="form-group"><label class="form-label"><i class="fas fa-briefcase"></i> ตำแหน่ง</label>
            <select class="form-control" id="ciPosition">
              <option>ครู</option><option>รองผู้อำนวยการ</option><option>ผู้อำนวยการ</option><option>บุคลากร</option>
            </select>
          </div>
          <div class="form-group"><label class="form-label"><i class="fas fa-tasks"></i> ประเภทเวร</label>
            <select class="form-control" id="ciDutyType">
              <option>เวรประจำวัน</option><option>เวรเช้า</option><option>เวรเย็น</option>
              <option>เวรกลางคืน</option><option>เวรวันหยุด</option><option>เวรพิเศษ</option>
            </select>
          </div>
        </div>
        <div class="form-group"><label class="form-label"><i class="fas fa-map-pin"></i> สถานที่เวร</label>
          <select class="form-control" id="ciLocation">
            <option>หน้าโรงเรียน</option><option>ประตูหลัง</option><option>อาคาร 1</option>
            <option>อาคาร 2</option><option>โรงอาหาร</option><option>ห้องพักครู</option><option>อื่นๆ</option>
          </select>
        </div>
        <div class="form-group">
          <label class="form-label"><i class="fas fa-camera"></i> รูปหลักฐาน</label>
          <div class="photo-preview-box" id="photoBox" onclick="openCameraModal()">
            <img id="photoPreview" alt="รูปหลักฐาน">
            <div class="photo-placeholder">
              <i class="fas fa-camera" style="font-size:32px;color:var(--gray-400)"></i>
              <p style="color:var(--gray-600);font-size:13px">แตะเพื่อถ่ายรูป</p>
              <p style="color:var(--gray-400);font-size:11px">ใช้กล้องหน้า/หลังได้</p>
            </div>
          </div>
          <div style="display:flex;gap:8px;margin-top:6px;flex-wrap:wrap">
            <button type="button" class="btn btn-sm btn-primary" onclick="openCameraModal()"><i class="fas fa-camera"></i> เปิดกล้อง</button>
            <button type="button" class="btn btn-sm btn-outline" onclick="document.getElementById('photoFileInput').click()"><i class="fas fa-upload"></i> เลือกรูป</button>
            <button type="button" class="btn btn-sm" style="background:#eee;color:#666" onclick="clearPhoto()"><i class="fas fa-trash"></i> ล้าง</button>
          </div>
          <input type="file" id="photoFileInput" accept="image/*" style="display:none" onchange="handleFileSelect(event)">
        </div>
        <div class="form-group"><label class="form-label"><i class="fas fa-sticky-note"></i> หมายเหตุ</label>
          <textarea class="form-control" id="ciNotes" rows="2" placeholder="หมายเหตุเพิ่มเติม (ถ้ามี)"></textarea>
        </div>
        <button type="submit" class="btn btn-success btn-lg" style="width:100%">
          <i class="fas fa-check-circle"></i> บันทึกเข้าเวร
        </button>
      </form>
    </div>
    <div class="checkin-right">
      <div class="glass" style="border-left:4px solid var(--orange)">
        <div class="glass-header"><div class="glass-title"><div class="icon-box g3"><i class="fas fa-sign-out-alt"></i></div> บันทึกออกเวร</div></div>
        <div class="form-grid" style="gap:14px">
          <div class="form-group"><label class="form-label"><i class="fas fa-id-card"></i> รหัสครู</label><input class="form-control" id="coTeacherId" placeholder="รหัสครูที่ต้องการออกเวร"></div>
          <div class="form-group"><label class="form-label"><i class="fas fa-sticky-note"></i> หมายเหตุ</label><textarea class="form-control" id="coNotes" rows="2"></textarea></div>
          <button class="btn btn-warning" onclick="handleCheckOut()" style="width:100%"><i class="fas fa-sign-out-alt"></i> บันทึกออกเวร</button>
        </div>
      </div>
      <div class="glass">
        <div class="glass-header">
          <div class="glass-title"><div class="icon-box g6"><i class="fas fa-list-ul"></i></div> เวรวันนี้</div>
          <span class="badge badge-info" id="todayDutyCount">0 คน</span>
        </div>
        <div style="max-height:320px;overflow-y:auto" id="todayDutyList">
          <p style="text-align:center;color:var(--gray-400);padding:20px;font-size:13px">ไม่มีข้อมูลเวรวันนี้</p>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- CALENDAR -->
<div class="page" id="page-calendar">
  <div class="page-header">
    <div class="page-title"><div class="page-title-icon g2"><i class="fas fa-calendar-alt"></i></div><div><h2>ปฏิทินเวร</h2><p>ดูตารางเวรรายเดือน</p></div></div>
    <div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap">
      <button class="btn btn-outline btn-sm" onclick="changeCalMonth(-1)"><i class="fas fa-chevron-left"></i></button>
      <span id="calMonthLabel" style="font-family:'Kanit',sans-serif;font-weight:600;font-size:16px;min-width:140px;text-align:center"></span>
      <button class="btn btn-outline btn-sm" onclick="changeCalMonth(1)"><i class="fas fa-chevron-right"></i></button>
      <button class="btn btn-sm btn-primary" onclick="goCalToday()">วันนี้</button>
    </div>
  </div>
  <div class="glass">
    <div style="display:flex;gap:16px;margin-bottom:14px;flex-wrap:wrap">
      <div style="display:flex;align-items:center;gap:6px;font-size:12px"><div style="width:12px;height:12px;border-radius:3px;background:var(--teal)"></div> เข้าเวรแล้ว</div>
      <div style="display:flex;align-items:center;gap:6px;font-size:12px"><div style="width:12px;height:12px;border-radius:3px;background:var(--amber)"></div> รอเข้าเวร</div>
      <div style="display:flex;align-items:center;gap:6px;font-size:12px"><div style="width:12px;height:12px;border-radius:3px;background:var(--red)"></div> ขาดเวร</div>
    </div>
    <div class="calendar-grid">
      <div class="cal-header">อา</div><div class="cal-header">จ</div><div class="cal-header">อ</div>
      <div class="cal-header">พ</div><div class="cal-header">พฤ</div><div class="cal-header">ศ</div><div class="cal-header">ส</div>
    </div>
    <div class="calendar-grid" id="calendarGrid" style="margin-top:4px"></div>
  </div>
  <div class="glass" id="calDayDetail" style="display:none">
    <div class="glass-header">
      <div class="glass-title"><div class="icon-box g1"><i class="fas fa-calendar-day"></i></div> <span id="calDetailDate"></span></div>
      <button class="btn btn-sm" style="background:#eee;color:#666" onclick="document.getElementById('calDayDetail').style.display='none'"><i class="fas fa-times"></i></button>
    </div>
    <div id="calDetailContent"></div>
  </div>
</div>

<!-- SCHEDULE -->
<div class="page" id="page-schedule">
  <div class="page-header">
    <div class="page-title"><div class="page-title-icon g5"><i class="fas fa-table"></i></div><div><h2>ตารางเวรทั้งปี</h2></div></div>
    <div style="display:flex;gap:10px;flex-wrap:wrap">
      <select class="form-control" id="scheduleMonthFilter" style="width:160px" onchange="loadSchedule()"><option value="">ทุกเดือน</option></select>
      <button class="btn btn-primary" onclick="switchPage('addschedule')"><i class="fas fa-plus"></i> เพิ่มตาราง</button>
    </div>
  </div>
  <div class="glass">
    <div style="overflow-x:auto"><table class="data-table">
      <thead><tr><th>วันที่</th><th>วัน</th><th>รหัสครู</th><th>ชื่อครู</th><th>ประเภทเวร</th><th>สถานที่</th><th>เวลา</th><th>สถานะ</th><th>จัดการ</th></tr></thead>
      <tbody id="scheduleTableBody"></tbody>
    </table></div>
    <div id="noScheduleData" style="text-align:center;padding:40px;color:var(--gray-400)"><i class="fas fa-calendar-times" style="font-size:40px;margin-bottom:10px;display:block;opacity:.4"></i><p>ยังไม่มีตารางเวร</p></div>
  </div>
</div>

<!-- LOGS -->
<div class="page" id="page-logs">
  <div class="page-header">
    <div class="page-title"><div class="page-title-icon g6"><i class="fas fa-history"></i></div><div><h2>บันทึกการเข้าเวร</h2></div></div>
    <div style="display:flex;gap:10px;flex-wrap:wrap">
      <input type="month" class="form-control" id="logsMonthFilter" style="width:160px" onchange="loadLogs()">
      <input type="text" class="form-control" id="logsSearch" style="width:180px" placeholder="ค้นหาชื่อครู..." oninput="filterLogsTable()">
      <button class="btn btn-success" onclick="exportExcel()"><i class="fas fa-file-excel"></i> Export</button>
    </div>
  </div>
  <div class="glass">
    <div style="overflow-x:auto"><table class="data-table">
      <thead><tr><th>วันที่</th><th>วัน</th><th>ชื่อครู</th><th>ตำแหน่ง</th><th>ประเภทเวร</th><th>เข้าเวร</th><th>ออกเวร</th><th>GPS</th><th>ระยะห่าง</th><th>หลักฐาน</th><th>สถานะ</th><th>จัดการ</th></tr></thead>
      <tbody id="logsTableBody"></tbody>
    </table></div>
    <div id="noLogsData" style="text-align:center;padding:40px;color:var(--gray-400)"><i class="fas fa-inbox" style="font-size:40px;margin-bottom:10px;display:block;opacity:.4"></i><p>ยังไม่มีข้อมูล</p></div>
  </div>
</div>

<!-- STATS -->
<div class="page" id="page-stats">
  <div class="page-header">
    <div class="page-title"><div class="page-title-icon g4"><i class="fas fa-bar-chart"></i></div><div><h2>สถิติครู</h2></div></div>
    <input type="month" class="form-control" id="statsMonth" style="width:160px" onchange="loadStats()">
  </div>
  <div class="cards-row cards-3">
    <div class="stat-card g1"><i class="fas fa-users stat-card-icon"></i><div class="stat-card-label"><i class="fas fa-chalkboard-teacher"></i> ครูทั้งหมด</div><div class="stat-card-value" id="statsTotal">-</div><div class="stat-card-sub">คน</div></div>
    <div class="stat-card g2"><i class="fas fa-star stat-card-icon"></i><div class="stat-card-label"><i class="fas fa-trophy"></i> เข้าเวรมากสุด</div><div class="stat-card-value" id="statsTop">-</div><div class="stat-card-sub" id="statsTopName">-</div></div>
    <div class="stat-card g5"><i class="fas fa-chart-bar stat-card-icon"></i><div class="stat-card-label"><i class="fas fa-percentage"></i> อัตราเข้าเวร</div><div class="stat-card-value" id="statsRate">-%</div><div class="stat-card-sub">โดยเฉลี่ย</div></div>
  </div>
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:20px">
    <div class="glass"><div class="glass-header"><div class="glass-title"><div class="icon-box g5"><i class="fas fa-chart-bar"></i></div> Top 10 ผู้เข้าเวรมากสุด</div></div><canvas id="chartStatsTop" height="200"></canvas></div>
    <div class="glass"><div class="glass-header"><div class="glass-title"><div class="icon-box g1"><i class="fas fa-chart-pie"></i></div> สัดส่วนประเภทเวร</div></div><canvas id="chartStatsDuty" height="200"></canvas></div>
  </div>
  <div class="glass">
    <div class="glass-header"><div class="glass-title"><div class="icon-box g2"><i class="fas fa-table"></i></div> ตารางสถิติรายครู</div></div>
    <div style="overflow-x:auto"><table class="data-table">
      <thead><tr><th>อันดับ</th><th>รหัสครู</th><th>ชื่อ-นามสกุล</th><th>เข้าเวรทั้งหมด</th><th>ออกเวรครบ</th><th>อัตราการเข้าเวร</th></tr></thead>
      <tbody id="statsTableBody"></tbody>
    </table></div>
  </div>
</div>

<!-- REPORT -->
<div class="page" id="page-report">
  <div class="page-header">
    <div class="page-title"><div class="page-title-icon g4"><i class="fas fa-file-pdf"></i></div><div><h2>รายงาน PDF / Excel</h2></div></div>
  </div>
  <div class="checkin-grid">
    <div class="glass">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g4"><i class="fas fa-file-pdf"></i></div> รายงาน PDF</div></div>
      <div class="form-grid" style="gap:14px">
        <div class="form-group"><label class="form-label">เดือน/ปี</label><input type="month" class="form-control" id="reportMonth"></div>
        <div class="form-group"><label class="form-label">ประเภทรายงาน</label>
          <select class="form-control" id="reportType"><option value="monthly">รายงานเดือน</option><option value="summary">รายงานสรุป</option></select>
        </div>
        <button class="btn btn-danger" onclick="generatePDF()" style="width:100%"><i class="fas fa-file-pdf"></i> สร้างรายงาน PDF</button>
      </div>
    </div>
    <div class="glass">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g2"><i class="fas fa-file-excel"></i></div> Export Excel</div></div>
      <div class="form-grid" style="gap:14px">
        <div class="form-group"><label class="form-label">เดือน/ปี</label><input type="month" class="form-control" id="exportMonth"></div>
        <div class="form-group"><label class="form-label">ข้อมูล</label>
          <select class="form-control" id="exportDataType"><option value="logs">บันทึกการเข้าเวร</option><option value="schedule">ตารางเวร</option><option value="stats">สถิติรายครู</option></select>
        </div>
        <button class="btn btn-success" onclick="exportExcelReport()" style="width:100%"><i class="fas fa-download"></i> ดาวน์โหลด Excel</button>
      </div>
    </div>
  </div>
  <div class="glass" id="reportPreview" style="display:none">
    <div class="glass-header">
      <div class="glass-title"><div class="icon-box g4"><i class="fas fa-eye"></i></div> ตัวอย่างรายงาน</div>
      <button class="btn btn-sm btn-primary" onclick="printReport()"><i class="fas fa-print"></i> พิมพ์</button>
    </div>
    <div id="reportContent"></div>
  </div>
</div>

<!-- TEACHERS -->
<div class="page" id="page-teachers">
  <div class="page-header">
    <div class="page-title"><div class="page-title-icon g1"><i class="fas fa-users"></i></div><div><h2>ข้อมูลครู</h2></div></div>
    <button class="btn btn-primary" onclick="showAddTeacherForm()"><i class="fas fa-user-plus"></i> เพิ่มครู</button>
  </div>
  <div class="glass" id="addTeacherForm" style="display:none;border-left:4px solid var(--teal)">
    <div class="glass-header">
      <div class="glass-title"><div class="icon-box g1"><i class="fas fa-user-plus"></i></div> เพิ่มข้อมูลครู</div>
      <button class="btn btn-sm" style="background:#eee;color:#666" onclick="hideAddTeacherForm()"><i class="fas fa-times"></i></button>
    </div>
    <div class="form-grid form-grid-3" style="gap:14px">
      <div class="form-group"><label class="form-label">รหัสครู</label><input class="form-control" id="tId" placeholder="T001"></div>
      <div class="form-group"><label class="form-label">ชื่อ-นามสกุล</label><input class="form-control" id="tName" placeholder="ชื่อเต็ม"></div>
      <div class="form-group"><label class="form-label">ตำแหน่ง</label><input class="form-control" id="tPosition" placeholder="ครู/บุคลากร"></div>
      <div class="form-group"><label class="form-label">วิชาเอก</label><input class="form-control" id="tSubject"></div>
      <div class="form-group"><label class="form-label">โทรศัพท์</label><input class="form-control" id="tPhone" placeholder="08xxxxxxxx"></div>
      <div class="form-group"><label class="form-label">อีเมล</label><input class="form-control" id="tEmail"></div>
      <div class="form-group"><label class="form-label">Line ID</label><input class="form-control" id="tLine"></div>
      <div class="form-group"><label class="form-label">Telegram ID</label><input class="form-control" id="tTelegram" placeholder="@username"></div>
    </div>
    <div style="margin-top:16px"><button class="btn btn-primary" onclick="saveTeacher()"><i class="fas fa-save"></i> บันทึก</button></div>
  </div>
  <div class="glass">
    <div style="overflow-x:auto"><table class="data-table">
      <thead><tr><th>รหัส</th><th>ชื่อ-นามสกุล</th><th>ตำแหน่ง</th><th>วิชาเอก</th><th>โทรศัพท์</th><th>สถานะ</th></tr></thead>
      <tbody id="teachersTableBody"></tbody>
    </table></div>
    <div id="noTeachersData" style="text-align:center;padding:40px;color:var(--gray-400)"><i class="fas fa-user-slash" style="font-size:40px;margin-bottom:10px;display:block;opacity:.4"></i><p>ยังไม่มีข้อมูลครู</p></div>
  </div>
</div>

<!-- ADD SCHEDULE -->
<div class="page" id="page-addschedule">
  <div class="page-header">
    <div class="page-title"><div class="page-title-icon g5"><i class="fas fa-calendar-plus"></i></div><div><h2>เพิ่มตารางเวร</h2></div></div>
  </div>
  <div class="checkin-grid">
    <div class="glass" style="border-left:4px solid var(--purple)">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g5"><i class="fas fa-plus-circle"></i></div> เพิ่มเวรเดี่ยว</div></div>
      <div class="form-grid" style="gap:14px">
        <div class="form-grid form-grid-2">
          <div class="form-group"><label class="form-label">วันที่</label><input type="date" class="form-control" id="scDate"></div>
          <div class="form-group"><label class="form-label">ประเภทเวร</label>
            <select class="form-control" id="scDutyType"><option>เวรประจำวัน</option><option>เวรเช้า</option><option>เวรเย็น</option><option>เวรกลางคืน</option><option>เวรวันหยุด</option></select>
          </div>
        </div>
        <div class="form-grid form-grid-2">
          <div class="form-group"><label class="form-label">รหัสครู</label><input class="form-control" id="scTeacherId" placeholder="T001"></div>
          <div class="form-group"><label class="form-label">ชื่อครู</label><input class="form-control" id="scTeacherName" placeholder="ชื่อ-นามสกุล"></div>
        </div>
        <div class="form-group"><label class="form-label">สถานที่เวร</label>
          <select class="form-control" id="scLocation"><option>หน้าโรงเรียน</option><option>ประตูหลัง</option><option>อาคาร 1</option><option>อาคาร 2</option><option>โรงอาหาร</option></select>
        </div>
        <div class="form-grid form-grid-2">
          <div class="form-group"><label class="form-label">เวลาเริ่ม</label><input type="time" class="form-control" id="scStartTime" value="07:00"></div>
          <div class="form-group"><label class="form-label">เวลาสิ้นสุด</label><input type="time" class="form-control" id="scEndTime" value="17:00"></div>
        </div>
        <div class="form-group"><label class="form-label">หมายเหตุ</label><textarea class="form-control" id="scNotes" rows="2"></textarea></div>
        <button class="btn btn-primary" onclick="saveScheduleSingle()" style="width:100%"><i class="fas fa-save"></i> บันทึกตารางเวร</button>
      </div>
    </div>
    <div class="glass">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g1"><i class="fas fa-calendar-week"></i></div> เพิ่มเวรแบบช่วงเวลา</div></div>
      <div class="form-grid" style="gap:14px">
        <div class="form-grid form-grid-2">
          <div class="form-group"><label class="form-label">วันเริ่มต้น</label><input type="date" class="form-control" id="scRangeStart"></div>
          <div class="form-group"><label class="form-label">วันสิ้นสุด</label><input type="date" class="form-control" id="scRangeEnd"></div>
        </div>
        <div class="form-group">
          <label class="form-label">เลือกวันในสัปดาห์</label>
          <div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:6px">
            <label style="display:flex;align-items:center;gap:4px;font-size:13px"><input type="checkbox" id="dow0"> อา</label>
            <label style="display:flex;align-items:center;gap:4px;font-size:13px"><input type="checkbox" id="dow1" checked> จ</label>
            <label style="display:flex;align-items:center;gap:4px;font-size:13px"><input type="checkbox" id="dow2" checked> อ</label>
            <label style="display:flex;align-items:center;gap:4px;font-size:13px"><input type="checkbox" id="dow3" checked> พ</label>
            <label style="display:flex;align-items:center;gap:4px;font-size:13px"><input type="checkbox" id="dow4" checked> พฤ</label>
            <label style="display:flex;align-items:center;gap:4px;font-size:13px"><input type="checkbox" id="dow5" checked> ศ</label>
            <label style="display:flex;align-items:center;gap:4px;font-size:13px"><input type="checkbox" id="dow6"> ส</label>
          </div>
        </div>
        <div class="form-grid form-grid-2">
          <div class="form-group"><label class="form-label">รหัสครู</label><input class="form-control" id="scRangeTeacherId" placeholder="T001"></div>
          <div class="form-group"><label class="form-label">ชื่อครู</label><input class="form-control" id="scRangeTeacherName"></div>
        </div>
        <div class="form-group"><label class="form-label">ประเภทเวร</label>
          <select class="form-control" id="scRangeDutyType"><option>เวรประจำวัน</option><option>เวรเช้า</option><option>เวรเย็น</option></select>
        </div>
        <div class="form-group"><label class="form-label">สถานที่</label>
          <select class="form-control" id="scRangeLocation"><option>หน้าโรงเรียน</option><option>ประตูหลัง</option><option>อาคาร 1</option></select>
        </div>
        <button class="btn btn-success" onclick="saveScheduleRange()" style="width:100%"><i class="fas fa-calendar-plus"></i> เพิ่มตารางช่วงเวลา</button>
      </div>
    </div>
  </div>
</div>

<!-- SETTINGS -->
<div class="page" id="page-settings">
  <div class="page-header">
    <div class="page-title"><div class="page-title-icon g5"><i class="fas fa-cog"></i></div><div><h2>ตั้งค่าระบบ</h2></div></div>
  </div>
  <div class="glass" style="border-left:4px solid var(--teal);margin-bottom:20px">
    <div class="glass-header"><div class="glass-title"><div class="icon-box g6"><i class="fas fa-link"></i></div> Google Apps Script URL</div></div>
    <div class="form-group">
      <label class="form-label"><i class="fas fa-server"></i> Script URL (จาก GAS Deploy)</label>
      <input class="form-control" id="setScriptUrl" placeholder="https://script.google.com/macros/s/.../exec">
      <p style="font-size:12px;color:var(--gray-400);margin-top:4px">ใส่ URL จาก Google Apps Script → Deploy → Web App</p>
    </div>
  </div>
  <div class="glass" style="border-left:4px solid var(--teal);margin-bottom:20px">
    <div class="glass-header"><div class="glass-title"><div class="icon-box g1"><i class="fas fa-image"></i></div> โลโก้โรงเรียน</div></div>
    <div style="display:flex;align-items:center;gap:20px;flex-wrap:wrap">
      <div class="logo-wrap" style="width:80px;height:80px;border-radius:16px;border:2px solid var(--teal-light)">
        <img id="logoPreviewSettings" src="" alt="โลโก้" style="width:100%;height:100%;object-fit:cover;border-radius:14px;display:none">
        <div style="font-size:28px;color:var(--teal)"><i class="fas fa-school"></i></div>
      </div>
      <div>
        <div style="display:flex;gap:8px;flex-wrap:wrap">
          <input type="file" id="logoFileInput" accept="image/*" style="display:none" onchange="handleLogoUpload(event)">
          <button class="btn btn-sm btn-outline" onclick="document.getElementById('logoFileInput').click()"><i class="fas fa-upload"></i> เลือกรูปโลโก้</button>
          <input class="form-control" id="logoUrlInput" placeholder="หรือวาง URL รูปภาพ" style="width:240px;font-size:12px">
          <button class="btn btn-sm btn-primary" onclick="applyLogoUrl()"><i class="fas fa-check"></i> ใช้</button>
        </div>
      </div>
    </div>
  </div>
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:24px">
    <div class="glass">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g1"><i class="fas fa-school"></i></div> ข้อมูลโรงเรียน</div></div>
      <div class="form-grid" style="gap:14px">
        <div class="form-group"><label class="form-label">ชื่อโรงเรียน</label><input class="form-control" id="setSchoolName" placeholder="โรงเรียน..."></div>
        <div class="form-grid form-grid-2">
          <div class="form-group"><label class="form-label">Latitude</label><input class="form-control" id="setLat" placeholder="13.7563"></div>
          <div class="form-group"><label class="form-label">Longitude</label><input class="form-control" id="setLng" placeholder="100.5018"></div>
        </div>
        <div class="form-group"><label class="form-label">รัศมี GPS (เมตร)</label><input class="form-control" id="setRadius" placeholder="200" type="number"></div>
      </div>
    </div>
    <div class="glass">
      <div class="glass-header"><div class="glass-title"><div class="icon-box g6"><i class="fab fa-telegram"></i></div> Telegram</div></div>
      <div class="form-grid" style="gap:14px">
        <div class="form-group"><label class="form-label">Bot Token</label><input class="form-control" id="setTgToken" placeholder="123456:AAABBB..."></div>
        <div class="form-group"><label class="form-label">Chat ID</label><input class="form-control" id="setTgChat" placeholder="-100123456789"></div>
      </div>
    </div>
  </div>
  <div style="margin-top:0"><button class="btn btn-primary btn-lg" onclick="saveSettings()"><i class="fas fa-save"></i> บันทึกการตั้งค่า</button></div>
</div>

</main>

<script>
/* ═══════════════════════════════════════════════════════════
   CONFIG — แก้ GAS_URL ตรงนี้จุดเดียว
═══════════════════════════════════════════════════════════ */
const GAS_URL = 'https://script.google.com/macros/s/AKfycbwVEYD-PT1Axx77Ep3U609YvGZ0bQThLjsVnbFEH6AYKdTqu7oE9UMgtDlxY3sTJ3Bu/exec'; // ใส่ URL xxxxxx/exec  แล้วอัพเดทระบบอีกครั้งนะครับ
let SCRIPT_URL = GAS_URL;

/* ═══════════════════════════════════════════════════════════
   ADMIN AUTH — ประกาศครั้งเดียว ไม่ซ้ำ
═══════════════════════════════════════════════════════════ */
let adminToken = sessionStorage.getItem('adminToken') || '';
let _adminCallback = null;

function requireAdmin(callback) {
  if (adminToken) { callback(); return; }
  _adminCallback = callback;
  document.getElementById('adminModal').style.display = 'flex';
  document.getElementById('adminPwdInput').value = '';
  document.getElementById('adminPwdError').style.display = 'none';
  setTimeout(() => document.getElementById('adminPwdInput').focus(), 100);
}

function closeAdminModal() {
  document.getElementById('adminModal').style.display = 'none';
  _adminCallback = null;
}

async function verifyAdminPwd() {
  const pwd = document.getElementById('adminPwdInput').value.trim();
  if (!pwd) return;
  const btn = document.getElementById('adminSubmitBtn');
  btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> กำลังตรวจสอบ...';
  btn.disabled = true;

  // ส่ง password ใน data object เพื่อให้ GAS รับผ่าน params.data
  const res = await api({ action: 'verifyAdmin', password: pwd });

  btn.innerHTML = '<i class="fas fa-unlock"></i> เข้าสู่ระบบ';
  btn.disabled = false;

  if (res.status === 'success') {
    adminToken = res.token || 'ok';
    sessionStorage.setItem('adminToken', adminToken);
    closeAdminModal();
    if (_adminCallback) { _adminCallback(); _adminCallback = null; }
  } else {
    document.getElementById('adminPwdError').style.display = 'block';
    document.getElementById('adminPwdInput').focus();
    document.getElementById('adminPwdInput').select();
  }
}

/* ═══════════════════════════════════════════════════════════
   API HELPER
   - action ทั่วไป → GET ?action=xxx&data=JSON
   - มีรูป (photoBase64 ขนาดใหญ่) → POST text/plain
═══════════════════════════════════════════════════════════ */
async function api(payload) {
  if (!SCRIPT_URL) {
    toast('กรุณาตั้งค่า Script URL ในหน้าตั้งค่าก่อน', 'error');
    return { status: 'error', message: 'No Script URL' };
  }

  const { action, ...data } = payload;
  const hasPhoto = data.photoBase64 && data.photoBase64.length > 500;

  try {
    if (hasPhoto) {
      // POST สำหรับ payload ใหญ่ (base64 รูป)
      await fetch(SCRIPT_URL, {
        method: 'POST',
        redirect: 'follow',
        headers: { 'Content-Type': 'text/plain;charset=utf-8' },
        body: JSON.stringify(payload)
      });
      return { status: 'success', photoUrl: '', gpsStatus: data.gpsStatus || '' };
    } else {
      // GET สำหรับ request ทั่วไป
      const params = new URLSearchParams({ action, data: JSON.stringify(data) });
      const res = await fetch(`${SCRIPT_URL}?${params}`, { redirect: 'follow' });
      const text = await res.text();
      try { return JSON.parse(text); }
      catch(e) { return { status: 'error', message: 'Parse error: ' + text.substring(0, 100) }; }
    }
  } catch (e) {
    console.error('API Error:', e);
    // Fallback: POST text/plain
    try {
      const res2 = await fetch(SCRIPT_URL, {
        method: 'POST',
        redirect: 'follow',
        headers: { 'Content-Type': 'text/plain;charset=utf-8' },
        body: JSON.stringify(payload)
      });
      const t2 = await res2.text();
      return JSON.parse(t2);
    } catch (e2) {
      toast('เชื่อมต่อ GAS ไม่ได้: ' + e2.message, 'error');
      return { status: 'error', message: e2.message };
    }
  }
}

/* ═══════════════════════════════════════════════════════════
   DATE / TIME HELPERS
═══════════════════════════════════════════════════════════ */
function todayStr() {
  const n = new Date();
  return n.getFullYear() + '-' + String(n.getMonth()+1).padStart(2,'0') + '-' + String(n.getDate()).padStart(2,'0');
}
function formatDate(v) {
  if (!v) return '-';
  const s = String(v).substring(0,10);
  if (!/^\d{4}-\d{2}-\d{2}$/.test(s)) return v;
  const [y,m,d] = s.split('-');
  return `${d}/${m}/${y}`;
}
function formatTime(v) {
  if (!v || v === '-') return '-';
  const s = String(v).trim();
  if (/^\d{1,2}:\d{2}/.test(s)) return s.substring(0,5);
  if (s.includes('T')) {
    try { const d=new Date(s); return String(d.getUTCHours()).padStart(2,'0')+':'+String(d.getUTCMinutes()).padStart(2,'0'); }
    catch(e){}
  }
  return s;
}
function normalizeDate(v) {
  if (!v) return '';
  if (v instanceof Date) return v.getFullYear()+'-'+String(v.getMonth()+1).padStart(2,'0')+'-'+String(v.getDate()).padStart(2,'0');
  const s = String(v).trim();
  if (/^\d{4}-\d{2}-\d{2}$/.test(s)) return s;
  if (/^\d{4}-\d{2}-\d{2}T/.test(s)) return s.substring(0,10);
  return s;
}

/* ═══════════════════════════════════════════════════════════
   CAMERA
═══════════════════════════════════════════════════════════ */
let camStream = null;
let camFacing = 'environment';

async function openCameraModal() {
  document.getElementById('cameraModal').classList.add('open');
  await startCamStream();
}
async function startCamStream() {
  if (camStream) { camStream.getTracks().forEach(t => t.stop()); camStream = null; }
  try {
    camStream = await navigator.mediaDevices.getUserMedia({
      video: { facingMode: camFacing, width: { ideal: 1280 }, height: { ideal: 720 } },
      audio: false
    });
    document.getElementById('camVideo').srcObject = camStream;
    document.getElementById('camFacingLabel').textContent = camFacing === 'environment' ? '(กล้องหลัง)' : '(กล้องหน้า)';
  } catch (e) {
    toast('ไม่สามารถเปิดกล้องได้: ' + e.message, 'error');
    closeCameraModal();
    document.getElementById('photoFileInput').click();
  }
}
async function switchCamera() {
  camFacing = camFacing === 'environment' ? 'user' : 'environment';
  await startCamStream();
}
function captureFromCamera() {
  const video = document.getElementById('camVideo');
  const canvas = document.getElementById('camCanvas');
  canvas.width = video.videoWidth || 640;
  canvas.height = video.videoHeight || 480;
  canvas.getContext('2d').drawImage(video, 0, 0);
  setPhotoPreview(canvas.toDataURL('image/jpeg', 0.85));
  closeCameraModal();
  toast('ถ่ายรูปสำเร็จ ✅', 'success');
}
function closeCameraModal() {
  if (camStream) { camStream.getTracks().forEach(t => t.stop()); camStream = null; }
  document.getElementById('cameraModal').classList.remove('open');
}
function handleFileSelect(e) {
  const file = e.target.files[0]; if (!file) return;
  const reader = new FileReader();
  reader.onload = ev => { setPhotoPreview(ev.target.result); toast('เลือกรูปสำเร็จ ✅', 'success'); };
  reader.readAsDataURL(file);
}
function setPhotoPreview(dataUrl) {
  document.getElementById('ciPhotoBase64').value = dataUrl;
  const img = document.getElementById('photoPreview');
  img.src = dataUrl;
  document.getElementById('photoBox').classList.add('has-photo');
}
function clearPhoto() {
  document.getElementById('ciPhotoBase64').value = '';
  document.getElementById('photoPreview').src = '';
  document.getElementById('photoBox').classList.remove('has-photo');
}

/* ═══════════════════════════════════════════════════════════
   GPS
═══════════════════════════════════════════════════════════ */
function getGPS() {
  const statusEl = document.getElementById('gpsStatusText');
  const coordEl  = document.getElementById('gpsCoordText');
  statusEl.textContent = '⏳ กำลังรับตำแหน่ง...';
  if (!navigator.geolocation) { statusEl.textContent = '❌ ไม่รองรับ GPS'; return; }
  navigator.geolocation.getCurrentPosition(pos => {
    const { latitude: lat, longitude: lng, accuracy: acc } = pos.coords;
    document.getElementById('ciLat').value = lat;
    document.getElementById('ciLng').value = lng;
    statusEl.textContent = '✅ ได้รับตำแหน่งแล้ว';
    coordEl.textContent  = `${lat.toFixed(5)}, ${lng.toFixed(5)} ±${Math.round(acc)}ม.`;
    const card = document.getElementById('gpsCard');
    card.style.background = 'linear-gradient(135deg,#E8F5E9,#C8E6C9)';
    card.style.borderColor = '#81C784';
  }, err => {
    statusEl.textContent = '❌ รับตำแหน่งไม่ได้';
    const card = document.getElementById('gpsCard');
    card.style.background = 'linear-gradient(135deg,#FFEBEE,#FFCDD2)';
    card.style.borderColor = '#EF9A9A';
  }, { enableHighAccuracy: true, timeout: 10000, maximumAge: 0 });
}

/* ═══════════════════════════════════════════════════════════
   SIDEBAR / NAVIGATION
═══════════════════════════════════════════════════════════ */
function toggleSidebar() {
  document.getElementById('sidebar').classList.toggle('open');
  document.getElementById('sidebarOverlay').classList.toggle('active');
}
function closeSidebar() {
  document.getElementById('sidebar').classList.remove('open');
  document.getElementById('sidebarOverlay').classList.remove('active');
}
function setBottomActive(el) {
  document.querySelectorAll('.bottom-nav-item').forEach(i => i.classList.remove('active'));
  el.classList.add('active');
}
function switchPage(id) {
  if (id === 'settings') { requireAdmin(() => _doSwitchPage('settings')); return; }
  _doSwitchPage(id);
}
function _doSwitchPage(id) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
  const page = document.getElementById('page-' + id);
  if (page) page.classList.add('active');
  const navEl = document.querySelector(`.nav-item[onclick="switchPage('${id}')"]`);
  if (navEl) navEl.classList.add('active');
  closeSidebar();
  const map = {
    dashboard: loadDashboard,
    checkin:   loadTodayDuty,
    calendar:  renderCalendar,
    schedule:  loadSchedule,
    logs:      loadLogs,
    stats:     loadStats,
    teachers:  loadTeachers
  };
  if (map[id]) map[id]();
}

/* ═══════════════════════════════════════════════════════════
   LOGO
═══════════════════════════════════════════════════════════ */
function handleLogoUpload(e) {
  const file = e.target.files[0]; if (!file) return;
  const r = new FileReader();
  r.onload = ev => {
    applyLogo(ev.target.result);
    const prev = document.getElementById('logoPreviewSettings');
    prev.src = ev.target.result; prev.style.display = 'block';
  };
  r.readAsDataURL(file);
}
function applyLogoUrl() {
  const url = document.getElementById('logoUrlInput').value.trim(); if (!url) return;
  applyLogo(url);
  const prev = document.getElementById('logoPreviewSettings');
  prev.src = url; prev.style.display = 'block';
}
function applyLogo(url) {
  const img = document.getElementById('logoImg');
  img.src = url; img.style.display = 'block';
  document.getElementById('logoFallback').style.display = 'none';
  localStorage.setItem('schoolLogo', url);
}

/* ═══════════════════════════════════════════════════════════
   INIT
═══════════════════════════════════════════════════════════ */
let allLogs = [], allSchedule = [], allTeachers = [];
let calYear = new Date().getFullYear(), calMonth = new Date().getMonth();
let charts = {};

window.onload = async () => {
  loadLocalSettings();
  setDefaultDates();
  showLoading('กำลังเริ่มต้นระบบ...');
  await loadRemoteConfig();
  await api({ action: 'init' });
  await Promise.all([loadDashboard(), loadTeachers()]);
  hideLoading();
  toast('ยินดีต้อนรับสู่ระบบครูเวร 🎉', 'success');
};

function loadLocalSettings() {
  const logo = localStorage.getItem('schoolLogo');
  if (logo) applyLogo(logo);

  const url = localStorage.getItem('scriptUrl') || GAS_URL;
  SCRIPT_URL = url || GAS_URL;
  const urlInput = document.getElementById('setScriptUrl');
  if (urlInput) urlInput.value = SCRIPT_URL;

  const name = localStorage.getItem('schoolName') || '';
  if (name) {
    document.getElementById('schoolNameDisplay').textContent = name;
    const ni = document.getElementById('setSchoolName'); if (ni) ni.value = name;
  }
  ['schoolLat:setLat','schoolLng:setLng','gpsRadius:setRadius','tgToken:setTgToken','tgChat:setTgChat'].forEach(pair => {
    const [key, elId] = pair.split(':');
    const v = localStorage.getItem(key); const el = document.getElementById(elId);
    if (v && el) el.value = v;
  });
}

function setDefaultDates() {
  const today = todayStr(), thisMonth = today.substring(0,7);
  const set = (id, v) => { const e = document.getElementById(id); if (e) e.value = v; };
  set('scDate', today); set('logsMonthFilter', thisMonth);
  set('statsMonth', thisMonth); set('reportMonth', thisMonth); set('exportMonth', thisMonth);

  document.getElementById('dashDateDisplay').textContent =
    new Date().toLocaleDateString('th-TH', { weekday:'long', year:'numeric', month:'long', day:'numeric' });

  const select = document.getElementById('scheduleMonthFilter');
  for (let m = 0; m < 12; m++) {
    const d = new Date(new Date().getFullYear(), m, 1);
    const val = d.toISOString().substring(0,7);
    const opt = document.createElement('option');
    opt.value = val;
    opt.textContent = d.toLocaleDateString('th-TH', { month:'long', year:'numeric' });
    select.appendChild(opt);
  }
}

async function loadRemoteConfig() {
  try {
    const params = new URLSearchParams({ action: 'getConfig', data: '{}' });
    const res = await fetch(`${GAS_URL}?${params}`, { redirect: 'follow' });
    const data = await res.json();
    if (data.status === 'success') {
      SCRIPT_URL = GAS_URL;
      localStorage.setItem('scriptUrl', GAS_URL);
      if (data.schoolName) {
        document.getElementById('schoolNameDisplay').textContent = data.schoolName;
        const ni = document.getElementById('setSchoolName'); if (ni) ni.value = data.schoolName;
        localStorage.setItem('schoolName', data.schoolName);
      }
      document.getElementById('sysStatus').textContent = 'ออนไลน์';
    } else {
      document.getElementById('sysStatus').textContent = 'เชื่อมต่อไม่ได้';
    }
  } catch (e) {
    console.warn('getConfig failed:', e.message);
    document.getElementById('sysStatus').textContent = 'ออฟไลน์';
  }
}

/* ═══════════════════════════════════════════════════════════
   DASHBOARD
═══════════════════════════════════════════════════════════ */
async function loadDashboard() {
  showLoading();
  const res = await api({ action: 'getDashboard' });
  hideLoading();
  if (res.status !== 'success') return;
  document.getElementById('ds-total').textContent     = res.today?.total     || 0;
  document.getElementById('ds-checkedin').textContent = res.today?.checkedIn || 0;
  document.getElementById('ds-absent').textContent    = res.today?.absent    || 0;
  document.getElementById('ds-month').textContent     = res.month?.total     || 0;
  renderRecentLogs(res.recentLogs || []);
  renderWeeklyChart(res.last7Days || []);
  renderTodayChart(res.today || {});
}

function renderRecentLogs(logs) {
  const tbody = document.getElementById('recentLogsBody');
  if (!logs.length) { tbody.innerHTML = '<tr><td colspan="7" style="text-align:center;padding:24px;color:#9eb3b0">ยังไม่มีข้อมูล</td></tr>'; return; }
  tbody.innerHTML = logs.slice(0,10).map(l => `
    <tr>
      <td>${formatDate(l['วันที่'])}</td>
      <td><b>${l['ชื่อครู']||'-'}</b></td>
      <td>${l['ประเภทเวร']||'-'}</td>
      <td style="color:#43A047;font-weight:600">${formatTime(l['เวลาเข้าเวร'])}</td>
      <td style="color:#E53935;font-weight:600">${formatTime(l['เวลาออกเวร'])}</td>
      <td style="font-size:11px">${l['GPS สถานะ']||'-'}</td>
      <td>${getStatusBadge(l['สถานะ'])}</td>
    </tr>`).join('');
}

function renderWeeklyChart(data) {
  const ctx = document.getElementById('chartWeekly')?.getContext('2d');
  if (!ctx) return;
  if (charts.weekly) charts.weekly.destroy();
  charts.weekly = new Chart(ctx, { type:'bar', data:{ labels:data.map(d=>d.label),
    datasets:[{ label:'จำนวนผู้เข้าเวร', data:data.map(d=>d.count),
    backgroundColor:'rgba(38,166,154,.7)', borderColor:'#26A69A', borderWidth:2, borderRadius:8 }] },
    options:{ responsive:true, plugins:{ legend:{display:false} },
    scales:{ y:{beginAtZero:true,ticks:{stepSize:1}}, x:{grid:{display:false}} } } });
}

function renderTodayChart(today) {
  const ctx = document.getElementById('chartToday')?.getContext('2d');
  if (!ctx) return;
  if (charts.today) charts.today.destroy();
  charts.today = new Chart(ctx, { type:'doughnut', data:{
    labels:['เข้าเวรแล้ว','รอ/ขาดเวร'],
    datasets:[{ data:[today.checkedIn||0, today.absent||0],
    backgroundColor:['#26A69A','#EF5350'], borderWidth:3, borderColor:'#fff' }] },
    options:{ responsive:true, cutout:'65%', plugins:{ legend:{position:'bottom'} } } });
}

/* ═══════════════════════════════════════════════════════════
   CHECK-IN / CHECK-OUT
═══════════════════════════════════════════════════════════ */
async function loadTodayDuty() {
  const res = await api({ action: 'getSchedule' });
  if (res.status !== 'success') return;
  const today = todayStr();
  const items = (res.data || []).filter(s => String(s['วันที่']).substring(0,10) === today);
  document.getElementById('todayDutyCount').textContent = items.length + ' คน';
  const c = document.getElementById('todayDutyList');
  if (!items.length) { c.innerHTML = '<p style="text-align:center;color:var(--gray-400);padding:20px;font-size:13px">ไม่มีตารางเวรวันนี้</p>'; return; }
  c.innerHTML = items.map(item => `
    <div style="display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--gray-100)">
      <div style="width:36px;height:36px;border-radius:50%;background:var(--teal-pale);display:flex;align-items:center;justify-content:center;color:var(--teal);font-size:16px;flex-shrink:0"><i class="fas fa-user"></i></div>
      <div style="flex:1;min-width:0">
        <div style="font-weight:600;font-size:13px">${item['ชื่อครู']||'-'}</div>
        <div style="font-size:11px;color:var(--gray-600)">${item['ประเภทเวร']||''} • ${item['สถานที่เวร']||''} • <span style="color:var(--teal)">${item['เวลาเริ่ม']||''} – ${item['เวลาสิ้นสุด']||''}</span></div>
      </div>
      <div>${getStatusBadge(item['สถานะ'])}</div>
    </div>`).join('');
}

async function handleCheckIn(e) {
  e.preventDefault();
  const name = document.getElementById('ciTeacherName').value.trim();
  const id   = document.getElementById('ciTeacherId').value.trim();
  if (!name || !id) { toast('กรุณากรอกข้อมูลให้ครบถ้วน', 'error'); return; }
  showLoading('กำลังบันทึกการเข้าเวร...');
  const res = await api({
    action: 'checkIn',
    teacherId: id, teacherName: name,
    position:    document.getElementById('ciPosition').value,
    dutyType:    document.getElementById('ciDutyType').value,
    location:    document.getElementById('ciLocation').value,
    lat:         document.getElementById('ciLat').value,
    lng:         document.getElementById('ciLng').value,
    photoBase64: document.getElementById('ciPhotoBase64').value,
    notes:       document.getElementById('ciNotes').value
  });
  hideLoading();
  if (res.status === 'success') {
    toast(`✅ บันทึกเข้าเวรสำเร็จ! ${res.gpsStatus||''}`, 'success');
    document.getElementById('ciNotes').value = '';
    document.getElementById('ciLat').value = '';
    document.getElementById('ciLng').value = '';
    clearPhoto();
    const card = document.getElementById('gpsCard');
    card.style.background = ''; card.style.borderColor = '';
    document.getElementById('gpsStatusText').textContent = 'กดปุ่มเพื่อตรวจสอบ GPS';
    document.getElementById('gpsCoordText').textContent  = 'ยังไม่ได้รับตำแหน่ง';
    loadTodayDuty();
  } else toast('เกิดข้อผิดพลาด: ' + (res.message||''), 'error');
}

async function handleCheckOut() {
  const id = document.getElementById('coTeacherId').value.trim();
  if (!id) { toast('กรุณากรอกรหัสครู', 'error'); return; }
  showLoading('กำลังบันทึกออกเวร...');
  const res = await api({ action: 'checkOut', teacherId: id, notes: document.getElementById('coNotes').value });
  hideLoading();
  if (res.status === 'success') {
    toast('✅ บันทึกออกเวรสำเร็จ', 'success');
    document.getElementById('coTeacherId').value = '';
    document.getElementById('coNotes').value = '';
  } else toast('เกิดข้อผิดพลาด: ' + res.message, 'error');
}

/* ═══════════════════════════════════════════════════════════
   CALENDAR
═══════════════════════════════════════════════════════════ */
function renderCalendar() {
  updateCalLabel();
  const monthStr = calYear + '-' + String(calMonth+1).padStart(2,'0');
  api({ action: 'getCalendar', month: monthStr }).then(res => {
    buildCalGrid(res.schedule||[], res.logs||[]);
  });
}
function updateCalLabel() {
  document.getElementById('calMonthLabel').textContent =
    new Date(calYear, calMonth).toLocaleDateString('th-TH', { month:'long', year:'numeric' });
}
function buildCalGrid(schedule, logs) {
  const grid = document.getElementById('calendarGrid');
  grid.innerHTML = '';
  const firstDay    = new Date(calYear, calMonth, 1).getDay();
  const daysInMonth = new Date(calYear, calMonth+1, 0).getDate();
  const today       = todayStr();
  const prevDays    = new Date(calYear, calMonth, 0).getDate();
  for (let i = firstDay-1; i >= 0; i--) {
    const div = document.createElement('div'); div.className = 'cal-day other-month';
    div.innerHTML = `<div class="day-num">${prevDays-i}</div>`; grid.appendChild(div);
  }
  for (let d = 1; d <= daysInMonth; d++) {
    const dateStr = calYear+'-'+String(calMonth+1).padStart(2,'0')+'-'+String(d).padStart(2,'0');
    const daySched = schedule.filter(s => String(s['วันที่']).substring(0,10) === dateStr);
    const dayLogs  = logs.filter(l => String(l['วันที่']).substring(0,10) === dateStr);
    const div = document.createElement('div');
    div.className = 'cal-day' + (dateStr === today ? ' today' : '');
    div.onclick = () => showCalDay(dateStr, daySched, dayLogs);
    let dots = '';
    if (dayLogs.length) dots += `<span class="duty-dot" style="background:var(--teal)"></span>`;
    if (daySched.length > dayLogs.length) dots += `<span class="duty-dot" style="background:var(--amber)"></span>`;
    div.innerHTML = `<div class="day-num">${d}</div><div style="margin-top:4px">${dots}</div>${daySched.length?`<div style="font-size:9px;color:var(--gray-600);margin-top:2px">${daySched.length}เวร</div>`:''}`;
    grid.appendChild(div);
  }
}
function showCalDay(dateStr, schedule, logs) {
  document.getElementById('calDetailDate').textContent = formatDate(dateStr);
  document.getElementById('calDayDetail').style.display = 'block';
  const c = document.getElementById('calDetailContent');
  if (!schedule.length && !logs.length) { c.innerHTML = '<p style="text-align:center;padding:20px;color:var(--gray-400)">ไม่มีตารางเวรวันนี้</p>'; return; }
  c.innerHTML = `<div style="display:grid;grid-template-columns:1fr 1fr;gap:16px">
    <div><h4 style="font-size:13px;font-weight:700;color:var(--amber);margin-bottom:10px"><i class="fas fa-calendar"></i> ตารางเวร (${schedule.length})</h4>
      ${schedule.map(s=>`<div style="padding:8px;background:var(--gray-50);border-radius:8px;margin-bottom:6px;font-size:12px"><b>${s['ชื่อครู']||'-'}</b><br>${s['ประเภทเวร']||''} • ${s['สถานที่เวร']||''}<br><span style="color:var(--teal-dark)">${formatTime(s['เวลาเริ่ม'])} – ${formatTime(s['เวลาสิ้นสุด'])}</span></div>`).join('')||'<p style="font-size:12px;color:var(--gray-400)">ไม่มี</p>'}
    </div>
    <div><h4 style="font-size:13px;font-weight:700;color:var(--teal);margin-bottom:10px"><i class="fas fa-check"></i> บันทึกเข้าเวร (${logs.length})</h4>
      ${logs.map(l=>`<div style="padding:8px;background:var(--teal-pale);border-radius:8px;margin-bottom:6px;font-size:12px"><b>${l['ชื่อครู']||'-'}</b><br>เข้า ${formatTime(l['เวลาเข้าเวร'])} ออก ${formatTime(l['เวลาออกเวร'])}</div>`).join('')||'<p style="font-size:12px;color:var(--gray-400)">ไม่มี</p>'}
    </div>
  </div>`;
  document.getElementById('calDayDetail').scrollIntoView({ behavior: 'smooth' });
}
function changeCalMonth(delta) {
  calMonth += delta;
  if (calMonth > 11) { calMonth = 0; calYear++; }
  if (calMonth < 0)  { calMonth = 11; calYear--; }
  renderCalendar();
}
function goCalToday() { calYear = new Date().getFullYear(); calMonth = new Date().getMonth(); renderCalendar(); }

/* ═══════════════════════════════════════════════════════════
   SCHEDULE
═══════════════════════════════════════════════════════════ */
async function loadSchedule() {
  showLoading();
  const res = await api({ action: 'getSchedule', month: document.getElementById('scheduleMonthFilter').value });
  hideLoading(); allSchedule = res.data||[]; renderScheduleTable(allSchedule);
}
function renderScheduleTable(data) {
  const tbody = document.getElementById('scheduleTableBody'), noData = document.getElementById('noScheduleData');
  if (!data.length) { tbody.innerHTML = ''; noData.style.display = 'block'; return; }
  noData.style.display = 'none';
  tbody.innerHTML = data.map(r => `<tr>
    <td>${formatDate(r['วันที่'])}</td><td>${r['วันในสัปดาห์']||'-'}</td>
    <td><span class="badge badge-gray">${r['รหัสครู']||'-'}</span></td>
    <td><b>${r['ชื่อครู']||'-'}</b></td><td>${r['ประเภทเวร']||'-'}</td>
    <td>${r['สถานที่เวร']||'-'}</td>
    <td>${formatTime(r['เวลาเริ่ม'])} – ${formatTime(r['เวลาสิ้นสุด'])}</td>
    <td>${getStatusBadge(r['สถานะ'])}</td>
    <td><button class="btn btn-sm btn-danger" onclick="deleteScheduleRow('${r['ID']}')"><i class="fas fa-trash"></i></button></td>
  </tr>`).join('');
}
async function saveScheduleSingle() {
  const date = document.getElementById('scDate').value, name = document.getElementById('scTeacherName').value.trim();
  if (!date) { toast('กรุณาเลือกวันที่', 'error'); return; }
  if (!name) { toast('กรุณากรอกชื่อครู', 'error'); return; }
  showLoading();
  const res = await api({ action: 'addSchedule', entries: [{
    date, teacherId: document.getElementById('scTeacherId').value.trim(), teacherName: name,
    dutyType: document.getElementById('scDutyType').value,
    location: document.getElementById('scLocation').value,
    startTime: document.getElementById('scStartTime').value,
    endTime: document.getElementById('scEndTime').value,
    notes: document.getElementById('scNotes').value
  }] });
  hideLoading();
  if (res.status === 'success') toast('✅ บันทึกตารางเวรสำเร็จ', 'success');
  else toast('เกิดข้อผิดพลาด: ' + res.message, 'error');
}
async function saveScheduleRange() {
  const startVal = document.getElementById('scRangeStart').value, endVal = document.getElementById('scRangeEnd').value;
  const name = document.getElementById('scRangeTeacherName').value.trim(), id = document.getElementById('scRangeTeacherId').value.trim();
  if (!startVal) { toast('กรุณาเลือกวันเริ่มต้น', 'error'); return; }
  if (!name) { toast('กรุณากรอกชื่อครู', 'error'); return; }
  const start = new Date(startVal), end = new Date(endVal || startVal);
  const selDays = [0,1,2,3,4,5,6].filter(i => document.getElementById('dow'+i)?.checked);
  const entries = [];
  for (let d = new Date(start); d <= end; d.setDate(d.getDate()+1)) {
    if (selDays.includes(d.getDay())) {
      entries.push({ date: normalizeDate(new Date(d)), teacherId: id, teacherName: name,
        dutyType: document.getElementById('scRangeDutyType').value,
        location: document.getElementById('scRangeLocation').value,
        startTime: '07:00', endTime: '17:00' });
    }
  }
  if (!entries.length) { toast('ไม่พบวันที่ตรงกับที่เลือก', 'error'); return; }
  if (!confirm(`จะเพิ่มตารางเวร ${entries.length} รายการ ยืนยัน?`)) return;
  showLoading('กำลังบันทึก...');
  const res = await api({ action: 'addSchedule', entries });
  hideLoading();
  if (res.status === 'success') toast(res.message, 'success');
  else toast('เกิดข้อผิดพลาด', 'error');
}
async function deleteScheduleRow(id) {
  if (!confirm('ยืนยันการลบ?')) return;
  toast('ลบข้อมูลสำเร็จ', 'success'); loadSchedule();
}

/* ═══════════════════════════════════════════════════════════
   LOGS
═══════════════════════════════════════════════════════════ */
async function loadLogs() {
  showLoading();
  const res = await api({ action: 'getLogs', month: document.getElementById('logsMonthFilter').value });
  hideLoading(); allLogs = res.data||[]; renderLogsTable(allLogs);
}
function renderLogsTable(data) {
  const tbody = document.getElementById('logsTableBody'), noData = document.getElementById('noLogsData');
  if (!data.length) { tbody.innerHTML = ''; noData.style.display = 'block'; return; }
  noData.style.display = 'none';
  tbody.innerHTML = data.map(r => `<tr>
    <td>${formatDate(r['วันที่'])}</td><td>${r['วันในสัปดาห์']||'-'}</td>
    <td><b>${r['ชื่อครู']||'-'}</b><br><small style="color:var(--gray-400)">${r['รหัสครู']||''}</small></td>
    <td>${r['ตำแหน่ง']||'-'}</td><td>${r['ประเภทเวร']||'-'}</td>
    <td style="color:#43A047;font-weight:600">${formatTime(r['เวลาเข้าเวร'])}</td>
    <td style="color:#E53935;font-weight:600">${formatTime(r['เวลาออกเวร'])}</td>
    <td style="font-size:11px">${r['GPS สถานะ']||'-'}</td>
    <td style="font-size:11px">${r['ระยะห่างจากโรงเรียน(ม)']||'-'} ม.</td>
    <td>${r['URL รูปภาพ']?`<a href="${r['URL รูปภาพ']}" target="_blank" class="btn btn-sm btn-outline"><i class="fas fa-image"></i></a>`:'-'}</td>
    <td>${getStatusBadge(r['สถานะ'])}</td>
    <td><button class="btn btn-sm btn-danger" onclick="deleteLog('${r['ID']}')"><i class="fas fa-trash"></i></button></td>
  </tr>`).join('');
}
function filterLogsTable() {
  const q = document.getElementById('logsSearch').value.toLowerCase();
  renderLogsTable(allLogs.filter(l => (l['ชื่อครู']||'').toLowerCase().includes(q)));
}
async function deleteLog(id) {
  if (!confirm('ยืนยันการลบ?')) return;
  const res = await api({ action: 'deleteLog', id });
  if (res.status === 'success') { toast('ลบสำเร็จ', 'success'); loadLogs(); }
  else toast('เกิดข้อผิดพลาด', 'error');
}

/* ═══════════════════════════════════════════════════════════
   STATS
═══════════════════════════════════════════════════════════ */
async function loadStats() {
  showLoading();
  const res = await api({ action: 'getStats', month: document.getElementById('statsMonth').value });
  hideLoading();
  const data = res.data||[];
  document.getElementById('statsTotal').textContent = data.length;
  if (data.length) {
    document.getElementById('statsTop').textContent = data[0].checkedIn;
    document.getElementById('statsTopName').textContent = data[0].name;
    const ti = data.reduce((s,d)=>s+d.checkedIn,0), ta = data.reduce((s,d)=>s+d.total,0);
    document.getElementById('statsRate').textContent = ta ? Math.round(ti/ta*100)+'%' : '0%';
  }
  document.getElementById('statsTableBody').innerHTML = data.map((r,i) => `<tr>
    <td><span class="badge ${i===0?'badge-warning':i===1?'badge-info':'badge-gray'}">#${i+1}</span></td>
    <td><span class="badge badge-gray">${r.id||'-'}</span></td>
    <td><b>${r.name||'-'}</b></td><td>${r.checkedIn}</td><td>${r.checkedOut}</td>
    <td><div style="display:flex;align-items:center;gap:8px">
      <div class="progress" style="flex:1;height:6px"><div class="progress-bar" style="width:${r.total?Math.round(r.checkedIn/r.total*100):0}%;background:var(--teal)"></div></div>
      <span style="font-size:12px;font-weight:700;color:var(--teal)">${r.total?Math.round(r.checkedIn/r.total*100):0}%</span>
    </div></td>
  </tr>`).join('');
  renderStatsCharts(data);
}
function renderStatsCharts(data) {
  const top10 = data.slice(0,10);
  const c1 = document.getElementById('chartStatsTop')?.getContext('2d');
  if (c1) { if(charts.st) charts.st.destroy(); charts.st = new Chart(c1,{type:'bar',data:{labels:top10.map(d=>d.name),datasets:[{label:'เข้าเวร',data:top10.map(d=>d.checkedIn),backgroundColor:'rgba(38,166,154,.75)',borderRadius:6}]},options:{indexAxis:'y',responsive:true,plugins:{legend:{display:false}},scales:{x:{beginAtZero:true},y:{grid:{display:false}}}}}); }
  const c2 = document.getElementById('chartStatsDuty')?.getContext('2d');
  if (c2 && data.length) { if(charts.sd) charts.sd.destroy(); charts.sd = new Chart(c2,{type:'doughnut',data:{labels:['เข้าเวรแล้ว','ออกเวรครบ'],datasets:[{data:[data.reduce((s,d)=>s+d.checkedIn,0),data.reduce((s,d)=>s+d.checkedOut,0)],backgroundColor:['#26A69A','#7CB342'],borderWidth:3,borderColor:'#fff'}]},options:{responsive:true,cutout:'60%',plugins:{legend:{position:'bottom'}}}}); }
}

/* ═══════════════════════════════════════════════════════════
   TEACHERS
═══════════════════════════════════════════════════════════ */
async function loadTeachers() {
  const res = await api({ action: 'getTeachers' }); allTeachers = res.data||[];
  const tbody = document.getElementById('teachersTableBody'), noData = document.getElementById('noTeachersData');
  if (!allTeachers.length) { tbody.innerHTML = ''; noData.style.display = 'block'; return; }
  noData.style.display = 'none';
  tbody.innerHTML = allTeachers.map(t => `<tr>
    <td><span class="badge badge-gray">${t['รหัสครู']||'-'}</span></td>
    <td><b>${t['ชื่อ-นามสกุล']||'-'}</b></td><td>${t['ตำแหน่ง']||'-'}</td>
    <td>${t['วิชาเอก']||'-'}</td><td>${t['โทรศัพท์']||'-'}</td>
    <td>${t['สถานะ']==='ปกติ'?'<span class="badge badge-success">ปกติ</span>':`<span class="badge badge-danger">${t['สถานะ']}</span>`}</td>
  </tr>`).join('');
}
function showAddTeacherForm() { document.getElementById('addTeacherForm').style.display = 'block'; }
function hideAddTeacherForm() { document.getElementById('addTeacherForm').style.display = 'none'; }
async function saveTeacher() {
  const id = document.getElementById('tId').value.trim(), name = document.getElementById('tName').value.trim();
  if (!id) { toast('กรุณากรอกรหัสครู', 'error'); return; }
  if (!name) { toast('กรุณากรอกชื่อ-นามสกุล', 'error'); return; }
  showLoading('กำลังบันทึกข้อมูลครู...');
  const res = await api({ action: 'addTeacher', teacherId: id, name,
    position: document.getElementById('tPosition').value, subject: document.getElementById('tSubject').value,
    phone: document.getElementById('tPhone').value, email: document.getElementById('tEmail').value,
    lineId: document.getElementById('tLine').value, telegramId: document.getElementById('tTelegram').value });
  hideLoading();
  if (res.status === 'success') {
    toast('✅ เพิ่มครูสำเร็จ', 'success'); hideAddTeacherForm();
    ['tId','tName','tPosition','tSubject','tPhone','tEmail','tLine','tTelegram'].forEach(i => document.getElementById(i).value = '');
    loadTeachers();
  } else toast('เกิดข้อผิดพลาด: ' + (res.message||''), 'error');
}

/* ═══════════════════════════════════════════════════════════
   REPORT / EXPORT
═══════════════════════════════════════════════════════════ */
async function exportExcel() {
  showLoading('กำลังสร้างไฟล์...');
  const res = await api({ action: 'exportReport' }); hideLoading();
  if (res.status !== 'success' || !res.data?.length) { toast('ไม่มีข้อมูล', 'error'); return; }
  const ws = XLSX.utils.json_to_sheet(res.data), wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'บันทึกเวร');
  XLSX.writeFile(wb, `รายงานครูเวร_${todayStr()}.xlsx`);
  toast('ดาวน์โหลดสำเร็จ', 'success');
}
async function exportExcelReport() {
  const month = document.getElementById('exportMonth').value, type = document.getElementById('exportDataType').value;
  showLoading('กำลังสร้างรายงาน...');
  const actionMap = { logs: 'exportReport', schedule: 'getSchedule', stats: 'getStats' };
  const res = await api({ action: actionMap[type]||'exportReport', month }); hideLoading();
  if (!res.data?.length) { toast('ไม่มีข้อมูล', 'error'); return; }
  const ws = XLSX.utils.json_to_sheet(res.data), wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'รายงาน');
  XLSX.writeFile(wb, `รายงาน_${type}_${month}.xlsx`);
  toast('ดาวน์โหลดสำเร็จ', 'success');
}
async function generatePDF() {
  showLoading('กำลังสร้าง PDF...');
  const res = await api({ action: 'exportReport', month: document.getElementById('reportMonth').value });
  hideLoading();
  if (res.status !== 'success') { toast('สร้างรายงานไม่ได้', 'error'); return; }
  const data = res.data||[], preview = document.getElementById('reportPreview'), content = document.getElementById('reportContent');
  preview.style.display = 'block';
  content.innerHTML = `<div style="font-family:'Sarabun',sans-serif;padding:20px;max-width:800px;margin:auto">
    <div style="text-align:center;margin-bottom:24px;border-bottom:3px solid var(--teal);padding-bottom:16px">
      <h2 style="font-family:'Kanit',sans-serif;font-size:20px;color:var(--teal-dark)">รายงานสรุปการเวรครู</h2>
      <p style="color:var(--gray-600)">สร้างเมื่อ ${new Date().toLocaleDateString('th-TH')}</p>
    </div>
    <table style="width:100%;border-collapse:collapse;font-size:12px">
      <thead><tr style="background:#E0F2F1">${['วันที่','ชื่อครู','ตำแหน่ง','ประเภทเวร','เข้าเวร','ออกเวร','สถานะ','GPS'].map(h=>`<th style="padding:8px;border:1px solid #ccc;font-weight:700;color:var(--teal-dark)">${h}</th>`).join('')}</tr></thead>
      <tbody>${data.map((r,i)=>`<tr style="background:${i%2?'#fff':'#f9f9f9'}">
        <td style="padding:8px;border:1px solid #eee">${formatDate(r['วันที่'])}</td>
        <td style="padding:8px;border:1px solid #eee">${r['ชื่อครู']||'-'}</td>
        <td style="padding:8px;border:1px solid #eee">${r['ตำแหน่ง']||'-'}</td>
        <td style="padding:8px;border:1px solid #eee">${r['ประเภทเวร']||'-'}</td>
        <td style="padding:8px;border:1px solid #eee">${formatTime(r['เวลาเข้าเวร'])}</td>
        <td style="padding:8px;border:1px solid #eee">${formatTime(r['เวลาออกเวร'])}</td>
        <td style="padding:8px;border:1px solid #eee">${r['สถานะ']||'-'}</td>
        <td style="padding:8px;border:1px solid #eee;font-size:10px">${r['GPS สถานะ']||'-'}</td>
      </tr>`).join('')}</tbody>
    </table>
    <div style="margin-top:20px;text-align:right;font-size:11px;color:var(--gray-400)">จำนวนทั้งหมด: ${data.length} รายการ</div>
  </div>`;
  preview.scrollIntoView({ behavior: 'smooth' });
  toast('สร้างรายงานสำเร็จ - กดพิมพ์เพื่อบันทึก PDF', 'success');
}
function printReport() {
  const content = document.getElementById('reportContent').innerHTML;
  const w = window.open('','_blank');
  w.document.write(`<html><head><title>รายงานครูเวร</title><link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@400;600&display=swap" rel="stylesheet"><style>body{font-family:'Sarabun',sans-serif}@media print{body{margin:0}}</style></head><body>${content}</body></html>`);
  w.document.close(); setTimeout(() => { w.print(); w.close(); }, 1000);
}

/* ═══════════════════════════════════════════════════════════
   SETTINGS
═══════════════════════════════════════════════════════════ */
function saveSettings() {
  const url    = document.getElementById('setScriptUrl').value.trim();
  const name   = document.getElementById('setSchoolName').value.trim();
  const lat    = document.getElementById('setLat').value.trim();
  const lng    = document.getElementById('setLng').value.trim();
  const radius = document.getElementById('setRadius').value.trim();
  const tgToken= document.getElementById('setTgToken').value.trim();
  const tgChat = document.getElementById('setTgChat').value.trim();

  if (!url) { toast('กรุณาใส่ Script URL', 'error'); return; }

  SCRIPT_URL = url;
  localStorage.setItem('scriptUrl', url);
  if (name)    { localStorage.setItem('schoolName', name); document.getElementById('schoolNameDisplay').textContent = name; }
  if (lat)       localStorage.setItem('schoolLat',  lat);
  if (lng)       localStorage.setItem('schoolLng',  lng);
  if (radius)    localStorage.setItem('gpsRadius',  radius);
  if (tgToken)   localStorage.setItem('tgToken',    tgToken);
  if (tgChat)    localStorage.setItem('tgChat',     tgChat);

  toast('✅ บันทึกการตั้งค่าสำเร็จ! กำลังเชื่อมต่อ...', 'success');
  document.getElementById('sysStatus').textContent = 'กำลังเชื่อมต่อ...';
  api({ action: 'init' }).then(res => {
    document.getElementById('sysStatus').textContent = res.status === 'success' ? 'ออนไลน์' : 'เชื่อมต่อไม่ได้';
    if (res.status === 'success') { loadDashboard(); loadTeachers(); }
  });
}

/* ═══════════════════════════════════════════════════════════
   UI HELPERS
═══════════════════════════════════════════════════════════ */
function getStatusBadge(s) {
  const m = { 'เข้าเวรแล้ว':'badge-info','ออกเวรแล้ว':'badge-success','ขาดเวร':'badge-danger','รอเข้าเวร':'badge-warning','ปกติ':'badge-success' };
  return `<span class="badge ${m[s]||'badge-gray'}">${s||'-'}</span>`;
}
function showLoading(msg) {
  const el = document.getElementById('loadingOverlay'); el.style.display = 'flex';
  const t = el.querySelector('.loading-text'); if (t) t.textContent = msg||'กำลังโหลด...';
}
function hideLoading() { document.getElementById('loadingOverlay').style.display = 'none'; }
function toast(msg, type='info') {
  const c = document.getElementById('toastContainer');
  const t = document.createElement('div'); t.className = `toast ${type}`;
  const icons = { success:'fas fa-check-circle', error:'fas fa-times-circle', info:'fas fa-info-circle' };
  const colors = { success:'#43A047', error:'#E53935', info:'var(--teal)' };
  t.innerHTML = `<i class="${icons[type]||icons.info}" style="color:${colors[type]||colors.info};font-size:18px;flex-shrink:0"></i><span style="font-size:14px;font-weight:500">${msg}</span>`;
  c.appendChild(t);
  setTimeout(() => { t.style.animation = 'slideIn .3s ease reverse'; setTimeout(() => t.remove(), 300); }, 3500);
}
</script>
</body>
</html>
